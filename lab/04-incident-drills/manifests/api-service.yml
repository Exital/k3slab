apiVersion: v1
kind: Service
metadata:
  name: api-svc
  namespace: incident-lab
  labels:
    app: api
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - name: http
      port: 80
      targetPort: http
