apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-backend-ingress
  namespace: platform-lab
spec:
  # Lock down the backend only (keeps kubelet probes / other pods unaffected).
  podSelector:
    matchLabels:
      app: backend
  policyTypes:
    - Ingress
