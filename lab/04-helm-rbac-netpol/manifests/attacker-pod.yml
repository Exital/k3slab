apiVersion: v1
kind: Pod
metadata:
  name: attacker
  namespace: platform-lab
  labels:
    app: attacker
spec:
  containers:
    - name: attacker
      image: busybox:1.36
      command: ["sleep", "3600"]
  restartPolicy: Always
